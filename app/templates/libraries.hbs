<h1>Libraries</h1>

<div class="well">
	<ul class="nav nav-pills">
		{{#link-to 'libraries.index' tagName="li"}}
		<a href="">List all</a>{{/link-to}}
		{{#if session.isAuthenticated}}
    {{#link-to 'libraries.new' tagName="li"}}
		<a href="">Add new</a>{{/link-to}}
    {{/if}}

	</ul>
</div>

{{outlet}}