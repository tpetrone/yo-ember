<nav class='navbar navbar-inverse'>
  <div class='container-fluid'>
    <div class='navbar-header'>
      <button type='button' class='navbar-toggle collapsed' data-toggle='collapse' data-target='#main-navbar'>
        <span class='sr-only'>Toggle navigation</span>
        <span class='icon-bar'></span>
        <span class='icon-bar'></span>
        <span class='icon-bar'></span>
      </button>
      {{#link-to 'index' class='navbar-brand'}}Library App{{/link-to}}
    </div>

    <div class='collapse navbar-collapse' id='main-navbar'>
      <ul class='nav navbar-nav'>
        {{#nav-link-to 'index'}}Home{{/nav-link-to}}
        {{#nav-link-to 'libraries'}}Libraries{{/nav-link-to}}
        {{#nav-link-to 'authors'}}Authors{{/nav-link-to}}
        {{#nav-link-to 'books'}}Books{{/nav-link-to}}
        {{#nav-link-to 'about'}}About{{/nav-link-to}}
        {{#nav-link-to 'contact'}}Contact{{/nav-link-to}}
      </ul>

      {{#if session.isAuthenticated}}
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-extended="false">
            Admin<span class="caret"></span>
          </a>
          <ul class="dropdown-menu">
            {{#nav-link-to 'logged.admin.invitations'}}Invitations{{/nav-link-to}}
            {{#nav-link-to 'logged.admin.contacts'}}Contacts{{/nav-link-to}}
            {{#nav-link-to 'logged.admin.seeder'}}Seeder{{/nav-link-to}}
          </ul>
        </li>
      </ul>

      <ul class='nav navbar-nav navbar-right'>
        <a {{action 'invalidateSession'}}>Logout</a>
      </ul>
      
      {{else}}
      <ul class='nav navbar-nav navbar-right'>
      {{#nav-link-to 'sign-up'}}Sign up{{/nav-link-to}}
      </ul>
      <ul class='nav navbar-nav navbar-right'>
        {{#nav-link-to 'login'}}Login{{/nav-link-to}}
      </ul>
      {{/if}}
    </div>
  </div>
</nav>