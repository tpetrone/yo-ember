<h1>Contacts</h1>

<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th>E-mail</th>
      <th>Message</th>
    </tr>
  </thead>
  <tbody>
  {{#each model as |contact|}}
    <tr>
      <th>{{contact.id}}</th>
      <th><span class="email">{{contact.email}}</span></th>
      <th><span class="message">{{contact.message}}</span></th>
    </tr>
  {{/each}}
  </tbody>
</table>